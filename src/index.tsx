//import "./style";
import { h, render, Component } from "preact";

export default class App extends Component {
  render() {
    console.log("ssss");
    return (
      <div>
        <h1>Hello, World!</h1>
      </div>
    );
  }
}
